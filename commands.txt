cd C:\Users\diego\Desktop\Python\TradingAgent
GOLD — Intraday
python pipeline/intraday_pipeline_web.py --symbol GOLD --web-agent --analyst analyst_1

Arquivo gerado:

data\debug_llm\GOLD_analyst_1_latest_input.txt
GOLD — Swing completo

Atualiza H1/M15 e também H4/D1/W1:

python pipeline/swing_pipeline_web.py --symbol GOLD

Arquivo gerado:

data\debug_llm\GOLD_swing_latest_input.txt
GOLD — Swing rápido

Usa H1/M15 já coletados anteriormente e atualiza apenas os timeframes maiores:

python pipeline/swing_pipeline_web.py --symbol GOLD --skip-intraday-refresh
EURUSD — Intraday

Primeiro, deixe EURUSD habilitado no tradingagent.json:

python pipeline/intraday_pipeline_web.py --symbol EURUSD --web-agent --analyst analyst_1

Arquivo:

data\debug_llm\EURUSD_analyst_1_latest_input.txt
EURUSD — Swing
python pipeline/swing_pipeline_web.py --symbol EURUSD

Ou rápido:

python pipeline/swing_pipeline_web.py --symbol EURUSD --skip-intraday-refresh

Arquivo:

data\debug_llm\EURUSD_swing_latest_input.txt
Validação do JSON

Quando alterar os símbolos:

python -m json.tool .\tradingagent.json
Resumo mais rápido


## GOLD swing:
python pipeline/swing_pipeline_web.py --symbol GOLD

# Swing para rodar todos os símbolos do JSON
py .\pipeline\swing_pipeline_web.py

GOLD swing rápido:
p# Para rodar só o GOLD
py .\pipeline\swing_pipeline_web.py --symbol GOLD


## Intraday 
## LLM local
python pipeline/intraday_pipeline_web.py ` --symbol GOLD ` --analyst analyst_1


## 
py .\pipeline\intraday_pipeline_web.py --web-agent

## por Symbol 

py .\pipeline\intraday_pipeline_web.py ` --symbol GOLD ` --web-agent ` --analyst analyst_1

python pipeline/intraday_pipeline_web.py ` --symbol GOLD ` --web-agent ` --analyst analyst_1

#Sem inteligencia
py .\pipeline\intraday_pipeline_web.py --symbol GOLD --web-agent --analyst analyst_1 --skip-market-intelligence

## 2 Symbol - EU and Gold
py .\pipeline\intraday_pipeline_web.py ` --symbol GOLD ` --symbol EURUSD ` --web-agent
  

## Market intelligence.

py .\market_intelligence.py build  --symbol GOLD  --input-dir .\data --output .\data\intelligence\GOLD.json

 Get-Content .\data\intelligence\GOLD.json 

## D1 point-in-time + H1/M15/M5 research (broker day / 09:00-18:00 BRT)
py .\tools\study_d1_mtf_filter.py `
  --input-dir .\data `
  --symbol GOLD `
  --rules .\config\market_intelligence\GOLD_d1_intraday_rules.json `
  --output .\data\research\GOLD_d1_mtf_filter_report.json

Get-Content .\data\research\GOLD_d1_mtf_filter_report.json

## Full process - 15 days interval 

python tools/base_dados_candle.py ` --symbol GOLD ` --mode full_rebuild ` --timeframes M1 M5 M15 H1 H4
python tools/market_chronos_dataset.py --symbol GOLD

python .\tools\market_chronos_research_runner.py `
  --symbol GOLD `
  --anchor-tf M5


## 

python tools/market_chronos_dataset.py --symbol GOLD

python tools/market_chronos_dataset.py --symbol GOLD --input data/market_chronos/candle_base/consolidated/GOLD_candle_research.parquet

python tools/base_dados_candle.py --symbol GOLD --mode full_rebuild --timeframes M1 M5 M15 H1 H4


//
python .\tools\personal_trade_auditor.py `
  --symbol GOLD `
  --mt5-symbol XAUUSD `
  --data-symbol GOLD `
  --from-date 2026-07-01 `
  --to-date 2026-07-01 `
  --mt5-config .\config\personal_mt5.local.json

python .\tools\personal_trade_execution_audit.py `
  --symbol GOLD `
  --data-symbol GOLD

python .\tools\personal_risk_guard_builder.py `
  --symbol GOLD `
  --data-symbol GOLD

  Get-ChildItem .\data\personal_trade_auditor\GOLD\personal_trade_* | Remove-Item -Force  

## Research — Exp14 ad-hoc syntax fix
# Em Python <=3.11, não quebrar chamada .corr(...) dentro de expressão f-string.
# Use variável intermediária:
# rho_age_touch = accepted["l2_age_hours"].corr(accepted["prior_touch_episodes"], method="spearman")
# print(f"rho(age,touch_count)={rho_age_touch:+.4f}")
# Para execução por prompt direto/PowerShell here-string, sempre reenviar o bloco Exp14 completo já corrigido; não depender de substituição manual.

## Research — Exp14 checkpoint / Exp15 frozen
# Exp14: earliest exact-price lineage age and prior-touch memory do NOT add stable incremental probability beyond CorridorPosition.
# Strong confounding: Spearman(age, prior touch episodes)=+0.8501.
# Major covariate shift: TEST median lineage age ~3378.5h vs ~222.5h TRAIN / ~212.7h VALIDATION; touch median 35 vs 11 / 11.
# AGE_BASE improves TEST slightly but worsens/does not improve VALIDATION; MEMORY_EXTENDED is mixed and fails the frozen BOTH-splits criterion.
# Status: EXACT_PRICE_LINEAGE_AGE = NOT_CONFIRMED; PRIOR_L2_TOUCH_MEMORY = REJECTED_INCREMENTAL; TEST_CALIBRATION_DRIFT = PERSISTS.
# Exp15 frozen: CURRENT L2 CONFIRMATION RECENCY / STRUCTURAL REFRESH.
# Define latest exact same KIND/price confirm_time <= state_time and CurrentL2AgeHours = state_time - latest_confirm_time.
# Primary: GEOMETRY vs GEOMETRY + log1p(CurrentL2AgeHours), TRAIN-only fit; must improve both VALIDATION and TEST, especially day-cluster Brier/LogLoss.
# No thresholds, no rolling windows, no touch buckets, no D1/Clock/Z/Fib.

## Research — Exp15 checkpoint / Exp16 frozen
# Exp15: current-confirmation recency is almost identical to lineage age: Spearman(current_age,lineage_age)=+0.9889.
# Refresh is rare: refresh-span median=0h, Q75=0h, Q90=2.25h; confirmation_count median=1, Q75=1.
# TEST current L2 age median ~3374.4h vs ~212.6h TRAIN / ~212.7h VALIDATION: the recent regime is genuinely using very old all-history boundaries, not merely recently refreshed old price lineages.
# RECENCY_EXT worsens VALIDATION (Brier +0.000102, LogLoss +0.000109) and improves TEST only modestly (Brier -0.000563, LogLoss -0.001290); AUC falls slightly in all splits.
# Day-cluster Validation is neutral and TEST gain CIs still cross zero. Status: CURRENT_L2_RECENCY = REJECTED_INCREMENTAL; EXACT_PRICE_AGE_MEMORY_FAMILY = CLOSED; CORRIDOR_POSITION = PRESERVED; TEST_DRIFT = PERSISTS.
# Exp16 frozen: ACTIVE STRUCTURAL FRONTIER.
# A confirmed swing remains ACTIVE only while no closed M5 after its confirm_time has closed through it in its target direction (HIGH consumed by close>level; LOW consumed by close<level).
# At OUTSIDE_15 state_time choose nearest ACTIVE confirmed H4/H1/M15 swing ahead; consumed historical levels are ineligible forever.
# Primary questions: active-frontier coverage/source-TF/age stability; transition incidence; TRAIN-only geometry calibration evaluated on Validation/Test by AUC/Brier/LogLoss and day-cluster residual/skill.
# Do not tune consumption tolerance, use wicks, reactivate consumed levels, change fractal, or add D1/Clock/Z/Fib after inspection.

## Research — Exp16 checkpoint / Exp17 frozen
# Exp16 materially changes the structural representation: only 14.87% / 14.55% / 4.72% of legacy L2 are still active in TRAIN / VALIDATION / TEST; same-target rates are 16.75% / 15.69% / 4.72%.
# Active-frontier coverage is 90.59% / 92.73% / 100.00%; active L2 median distance is 1.7658 / 1.5742 / 1.6335 ATR, versus legacy median 0.1239 ATR; active median age is 12.67h / 16.00h / 17.46h, eliminating the legacy TEST age explosion.
# Transition incidence becomes ADVANCE 33.84% / 37.25% / 31.60%, RECAPTURE 55.61% / 51.47% / 54.72%, CENSORED 10.55% / 11.27% / 13.68%.
# TRAIN-only active geometry model: intercept=-0.075205, slope=+0.671355. AUC = .7649 / .7744 / .7970; Brier skill vs frozen TRAIN constant = +20.40% / +22.29% / +25.19%.
# Day-cluster skill is strongly positive in both VALIDATION and TEST with CIs fully >0 for Brier and LogLoss. Calibration residual is +2.52pp VAL and -5.94pp TEST, both CIs crossing zero.
# Raw active corridor geometry is already close to observed probability (36.49/38.61/39.03% predicted vs 37.83/41.99/36.61% observed), unlike the overconfident legacy geometry. Status: ACTIVE_STRUCTURAL_FRONTIER = STRONG_EXPLORATORY_REPRESENTATION; LEGACY_IMMORTAL_L2 = SUPERSEDED_FOR_THIS_BRANCH; RUNTIME_PROMOTION = NONE.
# Exp17 frozen: RECURSIVE ACTIVE FRONTIER / SECOND-STEP MARKOV TRANSPORTABILITY.
# After a first ADVANCE_ACTIVE_BOS, use consumed L2 as the new back boundary, select nearest ACTIVE L3 ahead at that transition_time, and ask whether the same corridor-position law predicts ADVANCE_L3 vs RECAPTURE_L2 within 120m.
# Primary transport test applies the Exp16 STEP1 TRAIN-fitted geometry coefficients without refitting to STEP2 Validation/Test; a STEP2 TRAIN-only fit is diagnostic comparison only. Preserve close-based consumption, no tolerance, no reactivation, no D1/Clock/Z/Fib.

## Research — Exp17 execution contract finalized
# STEP2 origin = first-step ADVANCE_ACTIVE_BOS close. At this close L2 is consumed and becomes the back/recapture boundary.
# L3 is selected strictly from swing instances confirmed by STEP2 state_time whose consume_time is either NaT or strictly > STEP2 state_time; levels consumed on the same M5 close are excluded.
# STEP2 geometry: d_back2 = oriented distance from STEP2 close back to L2; d_forward2 = oriented distance to nearest active L3; position2=d_back2/(d_back2+d_forward2).
# Competing event window remains frozen at 120m after STEP2 state_time: first close beyond L3 = ADVANCE_L3; first close back through L2 = RECAPTURE_L2; otherwise CENSORED.
# Dedup STEP2 structural states by step2_state_time|bias|L2|L3 (round(6)), keeping the latest originating STEP1 state_time only if convergence occurs.
# Primary transport model uses Exp16 STEP1 TRAIN coefficients regenerated from STEP1 TRAIN only, then applied unchanged to STEP2 TRAIN/VALIDATION/TEST. STEP2 TRAIN refit is diagnostic only and must not replace the transport model.
# Period is assigned by STEP2 state_time. Any non-censored label whose resolution_time crosses the next split boundary is excluded from model scoring and explicitly audited; no cross-boundary label enters a prior-period fit.
# No L4/L5, no tolerance, no wick consumption, no expiry/reactivation, no D1/Clock/Z/Fib/weekday, and no post-result thresholding.

## Research — Exp18 checkpoint / Exp19 frozen
# Exp18 pooled STEP1+STEP2 non-censored transitions and tested whether STEP identity adds stable probability beyond active-corridor geometry.
# BASE TRAIN-only: intercept=-0.066240, geometry=+0.576167. EXTENDED recovers STEP1 (-0.075205,+0.671355) and STEP2 (-0.056921,+0.399639) TRAIN fits via step intercept/slope shifts.
# Event-weighted EXTENDED improves TRAIN slightly, but worsens VALIDATION (Delta AUC=-0.0073, Delta Brier=+0.001649, Delta LogLoss=+0.004097) and TEST (Delta AUC=-0.0047, Delta Brier=+0.001054, Delta LogLoss=+0.004020).
# Day-cluster EXTENDED-over-BASE gains are non-significant in both OOS periods; CIs cross zero. Therefore STEP_IDENTITY_INCREMENTAL_PROBABILITY = REJECTED and STEP_SPECIFIC_TRAIN_RECALIBRATION = NOT_PROMOTED.
# Geometry ranking remains strong at both depths: STEP1 AUC .7649/.7744/.7970 and STEP2 AUC .7020/.7898/.8639 for TRAIN/VAL/TEST. This strengthens ACTIVE_CORRIDOR_GEOMETRY_DEPTH_TRANSPORTABILITY.
# Important unresolved diagnostic: matched-day BASE residual STEP2-minus-STEP1 is negative in all splits and CI<0: TRAIN -8.69pp [-17.26,-0.07], VAL -24.67pp [-36.45,-12.00], TEST -16.17pp [-28.71,-3.72]. Pooled TEST calibration residual also remains negative (BASE -11.30pp CI [-16.48,-6.23]). Thus strict homogeneous probability-kernel invariance is NOT confirmed even though explicit STEP identity fails OOS.
# Status: HOMOGENEOUS_MARKOV_KERNEL = STRENGTHENED_AS_PARSIMONIOUS_CANDIDATE_NOT_CONFIRMED; GEOMETRY_RANKING_DEPTH_INVARIANCE = STRONGLY_SUPPORTED; PAIRED_DAY_STEP_RESIDUAL_DIFFERENCE = PRESENT; RUNTIME_PROMOTION = NONE.
# Exp19 frozen: PRIOR-STATE MEMORY / MARKOV SUFFICIENCY TEST on STEP2 only. BASE: y2 ~ geo_logit2. MEMORY_EXTENDED: y2 ~ geo_logit2 + geo_logit1, where geo_logit1 is the causal prior active-corridor state that actually advanced into STEP2. Fit TRAIN only; evaluate unchanged on VALIDATION and TEST by AUC/Brier/LogLoss, day-cluster score gain and calibration residual. No interactions, no overshoot/speed/path features, no L4/L5, no D1/Clock/Z/Fib/weekday. If prior geometry adds stable OOS information, current corridor is not Markov-sufficient; if it fails, memoryless active-corridor representation is strengthened and the paired-day residual is treated as selection/day-level dependence rather than a stable depth coefficient.

## Research — Exp19 checkpoint / Exp20 frozen
# Exp19 prior-state memory fails decisively OOS. STEP2 BASE=current_geo only; MEMORY adds causal prior_geo from the STEP1 corridor that actually advanced into STEP2.
# Prior/current geometry are nearly independent (Spearman +0.0707 overall). TRAIN prior_geo coefficient=+0.224672 and improves TRAIN, but this does not generalize.
# VALIDATION MEMORY vs BASE: Delta AUC=-0.0409, Delta Brier=+0.007192, Delta LogLoss=+0.018366. TEST: Delta AUC=-0.0321, Delta Brier=+0.008351, Delta LogLoss=+0.014950.
# Day-cluster MEMORY gains are negative in both OOS periods (VAL Brier -0.007247 / LL -0.017003; TEST Brier -0.008435 / LL -0.014070), with only ~20-27% bootstrap probability of improvement. Memory also worsens TEST calibration residual from -7.43pp to -10.01pp.
# Status: PRIOR_CORRIDOR_GEOMETRY_MEMORY = REJECTED_INCREMENTAL; FIRST_ORDER_PATH_MEMORY = NOT_SUPPORTED; CURRENT_ACTIVE_CORRIDOR = STRENGTHENED_AS_MARKOV_SUFFICIENT_RANKING_STATE; EXACT_PROBABILITY_KERNEL = STILL_NOT_CONFIRMED; RUNTIME_PROMOTION = NONE.
# Exp20 frozen: DYNAMIC ONE-STEP MARKOV KERNEL. Replace the arbitrary 120m first-passage label with a causal M5 transition kernel inside frozen active corridors. At each closed M5 while still strictly between fixed back/forward boundaries, recompute current CorridorPosition from the current close and classify the next closed M5 as ADVANCE, STAY, or RECAPTURE. Stop the episode on first boundary exit. Fit a TRAIN-only multinomial/softmax kernel from current geometry only and evaluate unchanged on Validation/Test, with day/episode-cluster scoring. No lag, dwell-time, path, Clock/D1/Z/Fib, or L4/L5 features in Exp20. If current geometry alone transports as a one-step 3-state kernel, this is stronger evidence for a genuine Markov state representation; only afterward test dwell-time as a semi-Markov alternative.

## Research — Exp20 checkpoint / Exp21 frozen
# Exp20 replaced the arbitrary 120m label with next-contiguous-M5 transitions inside frozen active STEP1 corridors. Dynamic sample: TRAIN 5612 rows/596 episodes/207 days; VAL 1959/204/80; TEST 2096/212/73. STAY is ~90% in all splits.
# TRAIN-only softmax geometry kernel: ADVANCE vs STAY intercept=-3.517028, geometry=+0.857591; RECAPTURE vs STAY intercept=-3.813144, geometry=-0.844111. Opposite slopes are the expected structural signature: forward-exit hazard rises with corridor position while back-exit hazard falls.
# Multiclass kernel beats frozen TRAIN class-frequency baseline in all splits. Brier skill TRAIN/VAL/TEST = +10.42%/+7.47%/+6.28%; LogLoss skill = +19.19%/+18.35%/+17.76%. AUC ADVANCE=.8521/.8160/.8751 and AUC RECAPTURE=.8158/.8697/.8120.
# Day-cluster gain is strongly positive in both OOS periods: VAL Brier +.038486 CI [.011894,.081528], LL +.133729 CI [.079202,.207657]; TEST Brier +.034916 CI [.009157,.065027], LL +.125253 CI [.054634,.196758]. Episode-cluster gains are also strongly positive.
# Exit-side geometry is exceptionally strong and stable: Brier skill vs constant is +69.15%/+73.64%/+77.40% and AUC=.9576/.9640/.9830 for TRAIN/VAL/TEST. Exit-hazard skill is real but much weaker: +8.70%/+5.60%/+4.32% Brier skill.
# Important unresolved issue: row-weighted aggregate calibration looks close, but equal-day class residuals are large and significant OOS (VAL STAY -14.84pp, ADV +6.54pp, RECAP +8.30pp; TEST STAY -12.95pp, ADV +3.00pp, RECAP +9.95pp). This indicates heterogeneity in persistence/episode duration that current geometry alone does not calibrate at the day level.
# Status: ONE_STEP_ACTIVE_CORRIDOR_GEOMETRY_KERNEL = STRONG_EXPLORATORY_OOS_SIGNAL; EXIT_SIDE_GEOMETRY = VERY_STRONG_OOS_STRUCTURE; MEMORYLESS_EXACT_KERNEL = NOT_YET_CONFIRMED; DAY_LEVEL_PERSISTENCE_HETEROGENEITY = PRESENT; RUNTIME_PROMOTION = NONE.
# Exp21 frozen: SEMI-MARKOV DWELL-TIME TEST. Keep the same dynamic one-step episodes and current CorridorPosition. Define DwellBars causally as number of completed contiguous STAY transitions since episode origin before the current state (0 at origin, increment after each STAY); log_dwell=log1p(DwellBars). BASE softmax uses geo_logit only. SEMI_MARKOV extends both ADVANCE-vs-STAY and RECAPTURE-vs-STAY logits with log_dwell, TRAIN-only. Primary criterion: improve multiclass Brier/LogLoss in BOTH VAL and TEST with positive day-cluster support and materially reduce equal-day STAY/exit residuals. Exit-hazard decomposition is frozen secondary; exit-side remains diagnostic and must not be used to rescue the model. No dwell buckets/thresholds, interactions, alternate clocks, lag returns, D1/Clock/Z/Fib, or step-interval changes after inspection.